-- The access network, destination, and places layers are already prepared as
-- GeoJSON by generate.py. Tilemaker requires a process file even when it is
-- only tiling external GeoJSON layers.
function node_function()
end

function way_function()
end
